# Shared Backend Layer

Cross-cutting infrastructure used by modules:

- `auth`: authentication/authorization dependencies and policy helpers
- `db`: database session and migration integration
- `events`: domain event contracts
- `schemas`: shared API envelope and pagination schemas

